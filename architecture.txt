╔══════════════════════════════════════════════════════════════════════════════════════════════╗
║                                      Web Server Host                                         ║
║                                                                                              ║
║  ┌─────────────────────────────────────────────────────────────────────────────────────┐     ║
║  │                              Frontend (Custom UI)                                   │     ║
║  │  • Login page → issues session JWT                                                  │     ║
║  │  • MLflow-like UI showing only user's own + shared assets                           │     ║
║  │  • Hard Delete button (with confirmation gate + blocker warnings)                   │     ║
║  │  • Asset sharing panel (grant/revoke permissions)                                   │     ║
║  │  • Model version source status badges (AVAILABLE / SOURCE_DELETED)                  │     ║
║  └──────────────────────────────────┬──────────────────────────────────────────────────┘     ║
║                                     │                                                        ║
║  ┌──────────────────────────────────▼───────────────────────────────────────────────────┐    ║
║  │                         Web Backend (Extended Business Logic)                        │    ║
║  │                                                                                      │    ║
║  │  ┌──────────────────────┐   ┌────────────────────────┐   ┌────────────────────────┐  │    ║
║  │  │   MLflow Proxy       │   │   Hard Delete Service  │   │  Permission Service    │  │    ║
║  │  │   (pass-through)     │   │                        │   │                        │  │    ║
║  │  │                      │   │ • Checks model version │   │ • Grant / revoke       │  │    ║
║  │  │ Forwards standard    │   │   blockers             │   │   asset access         │  │    ║
║  │  │ MLflow API calls     │   │ • Blocks if Production │   │ • Calls Auth host      │  │    ║
║  │  │ to MLflow host       │   │   model depends on it  │   │   to update DB         │  │    ║
║  │  │ after auth check     │   │ • Confirmation gate for│   │ • Only MANAGE-level    │  │    ║
║  │  │                      │   │   non-prod blockers    │   │   users can share      │  │    ║
║  │  │                      │   │ • Calls MLflow host to │   └────────────────────────┘  │    ║
║  │  │                      │   │   soft-delete first    │                               │    ║
║  │  │                      │   │ • Then deletes from    │   ┌────────────────────────┐  │    ║
║  │  │                      │   │   MinIO directly       │   │  Retention Scheduler   │  │    ║
║  │  │                      │   │ • Updates model version│   │                        │  │    ║
║  │  │                      │   │   source_status flag   │   │ • Cron job runs nightly│  │    ║
║  │  │                      │   │ • Calls Auth host to   │   │ • Finds soft-deleted   │  │    ║
║  │  │                      │   │   remove asset record  │   │   items older than n   │  │    ║
║  │  └──────────────────────┘   └────────────────────────┘   │   days                 │  │    ║
║  │                                                          │ • Runs same hard-delete│  │    ║
║  │                                                          │   logic as manual flow │  │    ║
║  │                                                          │ • Skips Production     │  │    ║
║  │                                                          │   blockers, logs warn  │  │    ║
║  │                                                          └────────────────────────┘  │    ║
║  └─────────┬──────────────────────────┬─────────────────────────────┬───────────────────┘    ║
║            │ MLflow API calls         │ Hard delete / MinIO ops     │ Permission ops         ║
╚════════════│══════════════════════════│═════════════════════════════│════════════════════════╝
             │                          │                             │
             ▼                          │                             ▼
╔════════════════════════════╗          │               ╔══════════════════════════════╗
║     MLflow Host            ║          │               ║     Authorization Host       ║
║                            ║          │               ║                              ║
║  ┌──────────────────────┐  ║          │               ║  • Permission tables         ║
║  │  Nginx (port 443)    │  ║          │               ║  • Asset registry            ║
║  │  • TLS termination   │  ║          │               ║  • source_status column      ║
║  │  • JWT validation    │  ║          │               ║    on model version records  ║
║  │  • Rejects non-JWT   │  ║          │               ║  • APIs:                     ║
║  └──────────┬───────────┘  ║          │               ║    GET  /permission          ║
║             │              ║          │               ║    POST /asset               ║
║  ┌──────────▼───────────┐  ║          │               ║    PUT  /permission          ║
║  │   Smart Proxy        │  ║          │               ║    DELETE /asset             ║
║  │   (FastAPI)          │  ║          │               ╚══════════════════════════════╝
║  │                      │  ║          │
║  │ • Auth check (calls  │  ║          │
║  │   Auth host)         │  ║          │
║  │ • Routes to owner's  │  ║          │
║  │   MLflow server      │  ║          │
║  └──────────┬───────────┘  ║          │
║             │              ║          │
║  ┌──────────▼───────────┐  ║          │
║  │  Process Manager     │  ║          │
║  │                      │  ║          │
║  │  alice → :5101       │  ║          │
║  │  bob   → :5102       │  ║          │
║  │  ...                 │  ║          │
║  └──────────┬───────────┘  ║          │
║             │              ║          │
║  ┌──────────▼───────────┐  ║    ┌─────▼───────────────────────────────────┐
║  │  MLflow Servers      │  ║    │           MinIO                         │
║  │  (per-user process)  │  ║    │                                         │
║  └──────────┬───────────┘  ║    │  mlflow-artifacts/                      │
║             │              ║    │  ├── {user}/runs/{run_id}/              │
║  ┌──────────▼───────────┐  ║    │  └── {user}/models/{name}/v{n}/         │
║  │  PostgreSQL          │  ║    │       ↑                                 │
║  │                      │  ║    │  model-owned copy (ref-counted,         │
║  │  ├── alice (schema)  │  ║    │  independent of run lifecycle)          │
║  │  ├── bob   (schema)  │  ║    └─────────────────────────────────────────┘
║  │  └── ...             │  ║
║  └──────────────────────┘  ║
╚════════════════════════════╝

╔══════════════════════════════════════════════════════╗
║               Notebooks Host                         ║
║                                                      ║
║  Apache Zeppelin sessions                            ║
║                                                      ║
║  mlflow.set_tracking_uri("https://mlflow-host")      ║
║                  │                                   ║
║                  │ standard MLflow Python API only   ║
║                  │ (log, search, register, etc.)     ║
║                  │ no hard delete, no sharing ops    ║
║                  ▼                                   ║
║           MLflow Host (Nginx → Smart Proxy)          ║
╚══════════════════════════════════════════════════════╝